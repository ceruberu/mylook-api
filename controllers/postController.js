// const { checkEmailInUse, checkUsernameInUse, createUser, hashPassword, comparePassword, createToken, verifyToken, getUser } = require('../services/userServices');
const { } = require('../services/postServices');

module.exports = {
  newPost: async (req, res) => {
    
  }
};